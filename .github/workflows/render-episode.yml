name: Render autonomous episode

on:
  workflow_dispatch:
    inputs:
      seed:
        description: Deterministic episode seed
        required: true
        default: '20260916'
      population:
        description: Number of residents
        required: true
        default: '30'
      days:
        description: Simulated days
        required: true
        default: '7'
  push:
    paths:
      - 'render-request.txt'

permissions:
  contents: read

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Resolve episode settings
        id: episode
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            seed='${{ inputs.seed }}'
            population='${{ inputs.population }}'
            days='${{ inputs.days }}'
          else
            seed=$(sed -n 's/^seed=//p' render-request.txt | head -n1)
            population=$(sed -n 's/^population=//p' render-request.txt | head -n1)
            days=$(sed -n 's/^days=//p' render-request.txt | head -n1)
          fi

          [[ "$seed" =~ ^[0-9]+$ ]] || { echo "Invalid seed"; exit 1; }
          [[ "$population" =~ ^[0-9]+$ ]] || { echo "Invalid population"; exit 1; }
          [[ "$days" =~ ^[0-9]+$ ]] || { echo "Invalid days"; exit 1; }

          echo "seed=$seed" >> "$GITHUB_OUTPUT"
          echo "population=$population" >> "$GITHUB_OUTPUT"
          echo "days=$days" >> "$GITHUB_OUTPUT"

      - name: Install renderer dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ffmpeg \
            espeak-ng \
            xvfb \
            libasound2-dev \
            libx11-dev \
            libxrandr-dev \
            libxi-dev \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            libxcursor-dev \
            libxinerama-dev

      - name: Configure
        run: cmake -S . -B build -DSIMULATION_BUILD_VIEWER=ON -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --parallel

      - name: Generate autonomous picture track
        run: |
          xvfb-run -a ./build/simulation_episode \
            '${{ steps.episode.outputs.seed }}' \
            '${{ steps.episode.outputs.population }}' \
            '${{ steps.episode.outputs.days }}' \
            episode-output
          test -s episode-output/mason-block.mp4
          test -s episode-output/episode.json
          test -s episode-output/timeline.txt
          mv episode-output/mason-block.mp4 episode-output/mason-block-silent.mp4

      - name: Build final soundtrack and resident voices
        run: |
          python3 scripts/postprocess_episode_audio.py \
            episode-output/episode.json \
            episode-output/mason-block-silent.mp4 \
            episode-output/mason-block.mp4
          test -s episode-output/mason-block.mp4
          test -s episode-output/dialogue-transcript.txt
          rm episode-output/mason-block-silent.mp4

      - name: Verify finished episode streams
        run: |
          ffprobe -v error -select_streams v:0 -show_entries stream=codec_name,width,height -of default=nw=1 episode-output/mason-block.mp4
          ffprobe -v error -select_streams a:0 -show_entries stream=codec_name,sample_rate -of default=nw=1 episode-output/mason-block.mp4

      - name: Upload episode
        uses: actions/upload-artifact@v4
        with:
          name: mason-block-seed-${{ steps.episode.outputs.seed }}
          path: |
            episode-output/mason-block.mp4
            episode-output/episode.json
            episode-output/timeline.txt
            episode-output/dialogue-transcript.txt
          if-no-files-found: error
          retention-days: 14
